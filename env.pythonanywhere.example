# ============================================
# Archivo .env para PythonAnywhere
# ============================================
# Copia este archivo como .env en tu servidor
# y reemplaza los valores con los reales
# ============================================

# ========== CONFIGURACIÓN GENERAL ==========
SECRET_KEY=tu-secret-key-super-segura-genera-una-nueva-aqui
DEBUG=False
ALLOWED_HOSTS=tu-usuario.pythonanywhere.com

# ========== BASE DE DATOS (Opción 1: MySQL - Recomendado) ==========
# Descomentar estas líneas si usas MySQL en PythonAnywhere
DB_ENGINE=mysql
DB_NAME=tu_usuario$portfolio_db
DB_USER=tu_usuario
DB_PASSWORD=tu-password-mysql-de-pythonanywhere
DB_HOST=tu_usuario.mysql.pythonanywhere-services.com

# ========== BASE DE DATOS (Opción 2: PostgreSQL Externo) ==========
# Descomentar esta línea si usas Neon.tech u otro PostgreSQL externo
# DATABASE_URL=postgresql://usuario:password@host.neon.tech/database

# ========== BASE DE DATOS (Opción 3: SQLite) ==========
# Si no configuras ninguna de las anteriores, usará SQLite por defecto
# No necesitas configurar nada adicional para SQLite

# ========== CLOUDINARY (OBLIGATORIO) ==========
CLOUDINARY_CLOUD_NAME=tu-cloud-name-de-cloudinary
CLOUDINARY_API_KEY=tu-api-key-de-cloudinary
CLOUDINARY_API_SECRET=tu-api-secret-de-cloudinary

# ========== CORS - Orígenes permitidos ==========
# Separa múltiples URLs con comas (sin espacios)
CORS_ALLOWED_ORIGINS=https://tu-frontend.vercel.app,https://arturo.engineer

# ========== JWT (Opcional - usa los defaults si no los cambias) ==========
# JWT_ACCESS_TOKEN_LIFETIME=60
# JWT_REFRESH_TOKEN_LIFETIME=1440
# JWT_ALGORITHM=HS256
# JWT_SECRET_KEY=usa-el-mismo-que-SECRET_KEY-o-genera-otro

# ========== PRICING/CURRENCY (Opcional) ==========
# USD_TO_COP_RATE=4700
# FORCE_PRICE_CURRENCY=COP

# ============================================
# NOTAS IMPORTANTES:
# ============================================
# 1. NO incluyas espacios alrededor del signo =
# 2. NO uses comillas para los valores (a menos que el valor las contenga)
# 3. Para generar SECRET_KEY segura, usa:
#    python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
# 4. Este archivo NO debe subirse a GitHub (ya está en .gitignore)
# ============================================

